#include <bits/stdc++.h>
#include<string>
using namespace std;

int main() {
  
   //Lowercase;
     string s;
      getline(cin,s);

      transform(s.begin(),s.end(),s.begin(),::tolower);
      cout<<s;
      

   

    return 0;
}
